node_modules/*
test-results/*
dist/*
coverage/*

# Generated by scripts/generate-skill.js — must remain byte-identical to
# the generator output so the prepublishOnly `skill:check` step
# (`git diff --exit-code skill/SKILL.md`) succeeds. Prettier's JSONC
# trailing-comma insertion and table-column alignment otherwise drift the
# file away from generator output on every `npm run format`.
skill/SKILL.md

# Generated by scripts/generate-codemap.js — embedded JSON uses 2-space
# indent for token efficiency; prettier (configured to 4) would reformat
# it on every `npm run format`, breaking the `codemap:check` drift gate.
CODEMAP.md

# Generated by scripts/generate-mapping.js — table column widths and
# inline-code-cell spacing are generator-controlled; prettier would
# rewrap them on every `npm run format`, breaking the `mapping:check`
# drift gate (turned on in phase 2 of the API-mapping rollout).
docs/API-MAPPING.md

# Fixture sources for tests/generate-codemap.test.ts. These are exact
# inputs to the parser; formatter rewrites would change AST line numbers
# and break assertions.
tests/fixtures/codemap/**

# Long-form backlog/archive prose with nested numbered lists containing
# inline code spans; prettier's markdown handling destructively reflows
# these bullets (collapses multi-line items, mangles backtick-adjacent
# whitespace). These files are planning/history docs, not API contracts —
# drift protection is low-value vs. the reflow damage.
BACKLOG.md
BACKLOG-ARCHIVE.md

# Generated by scripts/generate-agents-md.js — must remain byte-identical to
# generator output for `agents-md:check` to pass. Prettier's markdown reflow
# would otherwise drift the committed file on every `npm run format`.
AGENTS.md

# Generated by scripts/generate-skill.js — 2-space YAML indentation with
# double-quoted strings matches the skill-renderer output; Prettier's 4-space
# reformat would immediately reintroduce drift the next time `npm run skill`
# runs, breaking `format:check` for contributors.
skill/reference/payload-schemas.yaml
